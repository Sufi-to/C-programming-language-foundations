#include<stdio.h>

int main (void){
    
    int num;
    scanf("%d", &num);
    int readpop;
    int count= 0;
    
    for(int i = 0; i<num; i++){
        scanf("%d", &readpop);
        if (readpop > 10000){
            count =count + 1;
        }
        
    }
    printf("%d", count);
}
